import { CurrencyGlobalPipe } from './currency-global.pipe';

describe('CurrencyGlobalPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyGlobalPipe();
    expect(pipe).toBeTruthy();
  });
});
