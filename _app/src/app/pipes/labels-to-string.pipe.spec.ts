import { LabelsToStringPipe } from './labels-to-string.pipe';

describe('LabelsToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelsToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
